# lastfm.py
This is a last.fm 'now playing' script for znc.
The script takes arguments `username` and `api_key` separated by a comma, where `username` is your last.fm username and `api_key` can be obtained from  [here](https://www.last.fm/api/).